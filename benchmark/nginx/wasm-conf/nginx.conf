daemon off;
master_process off;
worker_processes 1;
error_log stderr notice;
events { worker_connections 256; }
http {
    access_log off;
    include mime.types;
    default_type application/octet-stream;
    server { listen 8082; location / { root html; index index.html; } }
}
