diff --git a/.gitignore b/.gitignore
index 7e50886..0cd56a4 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,3 +1,5 @@
 /Makefile
 /objs/
 /tmp/
+objs/
+Makefile
diff --git a/auto/endianness b/auto/endianness
index 1b552b6..37cfb6f 100644
--- a/auto/endianness
+++ b/auto/endianness
@@ -31,8 +31,8 @@ ngx_test="$CC $CC_TEST_FLAGS $CC_AUX_FLAGS \
 
 eval "$ngx_test >> $NGX_AUTOCONF_ERR 2>&1"
 
-if [ -x $NGX_AUTOTEST ]; then
-    if $NGX_AUTOTEST >/dev/null 2>&1; then
+if [ -f $NGX_AUTOTEST ]; then
+    if ${NGX_RUNNER:-} $NGX_AUTOTEST >/dev/null 2>&1; then
         echo " little endian"
         have=NGX_HAVE_LITTLE_ENDIAN . auto/have
     else
diff --git a/auto/feature b/auto/feature
index 3561f59..6d9a511 100644
--- a/auto/feature
+++ b/auto/feature
@@ -47,13 +47,13 @@ ngx_feature_inc_path=
 eval "/bin/sh -c \"$ngx_test\" >> $NGX_AUTOCONF_ERR 2>&1"
 
 
-if [ -x $NGX_AUTOTEST ]; then
+if [ -f $NGX_AUTOTEST ]; then
 
     case "$ngx_feature_run" in
 
         yes)
             # /bin/sh is used to intercept "Killed" or "Abort trap" messages
-            if /bin/sh -c $NGX_AUTOTEST >> $NGX_AUTOCONF_ERR 2>&1; then
+            if /bin/sh -c "${NGX_RUNNER:-} $NGX_AUTOTEST" >> $NGX_AUTOCONF_ERR 2>&1; then
                 echo " found"
                 ngx_found=yes
 
@@ -68,14 +68,14 @@ if [ -x $NGX_AUTOTEST ]; then
 
         value)
             # /bin/sh is used to intercept "Killed" or "Abort trap" messages
-            if /bin/sh -c $NGX_AUTOTEST >> $NGX_AUTOCONF_ERR 2>&1; then
+            if /bin/sh -c "${NGX_RUNNER:-} $NGX_AUTOTEST" >> $NGX_AUTOCONF_ERR 2>&1; then
                 echo " found"
                 ngx_found=yes
 
                 cat << END >> $NGX_AUTO_CONFIG_H
 
 #ifndef $ngx_feature_name
-#define $ngx_feature_name  `$NGX_AUTOTEST`
+#define $ngx_feature_name  `${NGX_RUNNER:-} $NGX_AUTOTEST`
 #endif
 
 END
@@ -86,7 +86,7 @@ END
 
         bug)
             # /bin/sh is used to intercept "Killed" or "Abort trap" messages
-            if /bin/sh -c $NGX_AUTOTEST >> $NGX_AUTOCONF_ERR 2>&1; then
+            if /bin/sh -c "${NGX_RUNNER:-} $NGX_AUTOTEST" >> $NGX_AUTOCONF_ERR 2>&1; then
                 echo " not found"
 
             else
diff --git a/auto/include b/auto/include
index c1bd364..f75c54a 100644
--- a/auto/include
+++ b/auto/include
@@ -31,7 +31,7 @@ ngx_test="$CC -o $NGX_AUTOTEST $NGX_AUTOTEST.c"
 
 eval "$ngx_test >> $NGX_AUTOCONF_ERR 2>&1"
 
-if [ -x $NGX_AUTOTEST ]; then
+if [ -f $NGX_AUTOTEST ]; then
 
     ngx_found=yes
 
diff --git a/auto/types/sizeof b/auto/types/sizeof
index 480d8cf..fbc7d5a 100644
--- a/auto/types/sizeof
+++ b/auto/types/sizeof
@@ -39,8 +39,8 @@ ngx_test="$CC $CC_TEST_FLAGS $CC_AUX_FLAGS \
 eval "$ngx_test >> $NGX_AUTOCONF_ERR 2>&1"
 
 
-if [ -x $NGX_AUTOTEST ]; then
-    ngx_size=`$NGX_AUTOTEST`
+if [ -f $NGX_AUTOTEST ]; then
+    ngx_size=`${NGX_RUNNER:-} $NGX_AUTOTEST`
     echo " $ngx_size bytes"
 fi
 
diff --git a/auto/types/typedef b/auto/types/typedef
index d54c289..514506e 100644
--- a/auto/types/typedef
+++ b/auto/types/typedef
@@ -39,7 +39,7 @@ END
 
     eval "$ngx_test >> $NGX_AUTOCONF_ERR 2>&1"
 
-    if [ -x $NGX_AUTOTEST ]; then
+    if [ -f $NGX_AUTOTEST ]; then
         if [ $ngx_try = $ngx_type ]; then
             echo " found"
             ngx_found=yes
diff --git a/auto/types/uintptr_t b/auto/types/uintptr_t
index a33d6d0..6915cab 100644
--- a/auto/types/uintptr_t
+++ b/auto/types/uintptr_t
@@ -31,7 +31,7 @@ ngx_test="$CC $CC_TEST_FLAGS $CC_AUX_FLAGS \
 
 eval "$ngx_test >> $NGX_AUTOCONF_ERR 2>&1"
 
-if [ -x $NGX_AUTOTEST ]; then
+if [ -f $NGX_AUTOTEST ]; then
     echo " uintptr_t found"
     found=yes
 else
diff --git a/auto/unix b/auto/unix
index 4e85090..6e51f40 100644
--- a/auto/unix
+++ b/auto/unix
@@ -946,7 +946,12 @@ ngx_feature_incs="#include <sys/ioctl.h>
                   $NGX_INCLUDE_SYS_FILIO_H"
 ngx_feature_path=
 ngx_feature_libs=
-ngx_feature_test="int i = FIONREAD; printf(\"%d\", i)"
+# wasi-libc declares FIONREAD, but its Preview 2 ioctl returns ENOTSUP.
+# Keep nginx's normal non-FIONREAD recv/readv fallback on this target.
+ngx_feature_test="#if defined(__wasi__) || defined(__wasip2__)
+                 #error FIONREAD is not implemented by wasi-libc
+                 #endif
+                 int i = FIONREAD; printf(\"%d\", i)"
 . auto/feature
 
 
diff --git a/src/core/ngx_cycle.c b/src/core/ngx_cycle.c
index 410cc3c..f563a48 100644
--- a/src/core/ngx_cycle.c
+++ b/src/core/ngx_cycle.c
@@ -1259,6 +1259,7 @@ ngx_reopen_files(ngx_cycle_t *cycle, ngx_uid_t user)
 
                 fi.st_mode |= (S_IRUSR|S_IWUSR);
 
+#ifndef __wasi__
                 if (chmod((const char *) file[i].name.data, fi.st_mode) == -1) {
                     ngx_log_error(NGX_LOG_EMERG, cycle->log, ngx_errno,
                                   "chmod() \"%s\" failed", file[i].name.data);
@@ -1271,6 +1272,7 @@ ngx_reopen_files(ngx_cycle_t *cycle, ngx_uid_t user)
 
                     continue;
                 }
+#endif /* __wasi__: WASI 无权限位 */
             }
         }
 
diff --git a/src/core/ngx_file.c b/src/core/ngx_file.c
index 63ada85..13a6f35 100644
--- a/src/core/ngx_file.c
+++ b/src/core/ngx_file.c
@@ -642,11 +642,13 @@ ngx_create_paths(ngx_cycle_t *cycle, ngx_uid_t user)
         {
             fi.st_mode |= (S_IRUSR|S_IWUSR|S_IXUSR);
 
+#ifndef __wasi__
             if (chmod((const char *) path[i]->name.data, fi.st_mode) == -1) {
                 ngx_log_error(NGX_LOG_EMERG, cycle->log, ngx_errno,
                               "chmod() \"%s\" failed", path[i]->name.data);
                 return NGX_ERROR;
             }
+#endif /* __wasi__: WASI 无权限位 */
         }
         }
 #endif
diff --git a/src/http/ngx_http_request.c b/src/http/ngx_http_request.c
index ed9a7e8..cd31a97 100644
--- a/src/http/ngx_http_request.c
+++ b/src/http/ngx_http_request.c
@@ -3239,6 +3239,16 @@ ngx_http_test_reading(ngx_http_request_t *r)
 #endif
 
     n = recv(c->fd, buf, 1, MSG_PEEK);
+#ifdef __wasi__
+    if (n == -1 && (ngx_socket_errno == ENOSYS || ngx_socket_errno == ENOTSUP)) {
+        /* wasi:sockets 不支持 MSG_PEEK（返回 ENOTSUP），当作暂无数据。
+         * 注意：必须走 EAGAIN 路径完成 level-event 的读事件摘除——
+         * 若直接 return，探测过的连接事件会留在 select 集里，
+         * p2 select 对半关闭连接持续报就绪 → 忙循环（实测 CPU 67%、
+         * 第 2 轮压测起全部连接饿死）。 */
+        ngx_socket_errno = NGX_EAGAIN;
+    }
+#endif
 
     if (n == 0) {
         rev->eof = 1;
diff --git a/src/http/ngx_http_upstream.c b/src/http/ngx_http_upstream.c
index 3f86534..9ebc9f7 100644
--- a/src/http/ngx_http_upstream.c
+++ b/src/http/ngx_http_upstream.c
@@ -1509,6 +1509,16 @@ ngx_http_upstream_check_broken_connection(ngx_http_request_t *r,
 #endif
 
     n = recv(c->fd, buf, 1, MSG_PEEK);
+#ifdef __wasi__
+    if (n == -1 && (ngx_socket_errno == ENOSYS || ngx_socket_errno == ENOTSUP)) {
+        /* wasi:sockets 不支持 MSG_PEEK（返回 ENOTSUP），当作暂无数据。
+         * 注意：必须走 EAGAIN 路径完成 level-event 的读事件摘除——
+         * 若直接 return，探测过的连接事件会留在 select 集里，
+         * p2 select 对半关闭连接持续报就绪 → 忙循环（实测 CPU 67%、
+         * 第 2 轮压测起全部连接饿死）。 */
+        ngx_socket_errno = NGX_EAGAIN;
+    }
+#endif
 
     err = ngx_socket_errno;
 
diff --git a/src/os/unix/ngx_files.c b/src/os/unix/ngx_files.c
index 2fec1ec..35c34d6 100644
--- a/src/os/unix/ngx_files.c
+++ b/src/os/unix/ngx_files.c
@@ -37,6 +37,31 @@ ngx_read_file(ngx_file_t *file, u_char *buf, size_t size, off_t offset)
 
 #if (NGX_HAVE_PREAD)
 
+#if defined(__wasi__) || defined(__wasip2__)
+    /* Preview 2 may return a bounded chunk even for a regular file.
+     * Fill the requested buffer unless EOF or an actual error is reached. */
+    {
+        size_t  done;
+
+        done = 0;
+        while (done < size) {
+            n = pread(file->fd, buf + done, size - done, offset + done);
+            if (n == -1) {
+                if (ngx_errno == NGX_EINTR) {
+                    continue;
+                }
+                ngx_log_error(NGX_LOG_CRIT, file->log, ngx_errno,
+                              "pread() \"%s\" failed", file->name.data);
+                return NGX_ERROR;
+            }
+            if (n == 0) {
+                break;
+            }
+            done += n;
+        }
+        n = done;
+    }
+#else
     n = pread(file->fd, buf, size, offset);
 
     if (n == -1) {
@@ -44,6 +69,7 @@ ngx_read_file(ngx_file_t *file, u_char *buf, size_t size, off_t offset)
                       "pread() \"%s\" failed", file->name.data);
         return NGX_ERROR;
     }
+#endif
 
 #else
 
@@ -393,6 +419,26 @@ ngx_chain_to_iovec(ngx_iovec_t *vec, ngx_chain_t *cl)
 static ssize_t
 ngx_writev_file(ngx_file_t *file, ngx_iovec_t *vec, off_t offset)
 {
+#if defined(__wasi__) || defined(__wasip2__)
+    ssize_t     n, written;
+    ngx_uint_t  i;
+
+    /* wasi-libc's pwritev only processes the first iovec. Use nginx's
+     * positional write loop for every buffer, including short writes/EINTR. */
+    written = 0;
+    for (i = 0; i < vec->count; i++) {
+        if (vec->iovs[i].iov_len == 0) {
+            continue;
+        }
+        n = ngx_write_file(file, vec->iovs[i].iov_base,
+                           vec->iovs[i].iov_len, offset + written);
+        if (n == NGX_ERROR) {
+            return NGX_ERROR;
+        }
+        written += n;
+    }
+    return written;
+#else
     ssize_t    n;
     ngx_err_t  err;
 
@@ -470,6 +516,7 @@ eintr:
     file->offset += n;
 
     return n;
+#endif
 }
 
 
diff --git a/src/os/unix/ngx_readv_chain.c b/src/os/unix/ngx_readv_chain.c
index 48fa0a7..1d100a7 100644
--- a/src/os/unix/ngx_readv_chain.c
+++ b/src/os/unix/ngx_readv_chain.c
@@ -122,7 +122,24 @@ ngx_readv_chain(ngx_connection_t *c, ngx_chain_t *chain, off_t limit)
                    "readv: %ui, last:%uz", vec.nelts, iov->iov_len);
 
     do {
+#ifdef __wasi__
+        /* wasi-libc p2 的 readv 只读第一个 iov（实测），逐段 read 代替 */
+        {
+            struct iovec *iovs = (struct iovec *) vec.elts;
+            n = 0;
+            for (ngx_uint_t i = 0; i < vec.nelts; i++) {
+                ssize_t rc = read(c->fd, iovs[i].iov_base, iovs[i].iov_len);
+                if (rc <= 0) {
+                    if (n == 0) { n = rc; }
+                    break;
+                }
+                n += rc;
+                if ((size_t) rc < iovs[i].iov_len) break;
+            }
+        }
+#else
         n = readv(c->fd, (struct iovec *) vec.elts, vec.nelts);
+#endif
 
         if (n == 0) {
             rev->ready = 0;
diff --git a/src/os/unix/ngx_writev_chain.c b/src/os/unix/ngx_writev_chain.c
index e38a3aa..c4d6d9a 100644
--- a/src/os/unix/ngx_writev_chain.c
+++ b/src/os/unix/ngx_writev_chain.c
@@ -186,7 +186,41 @@ ngx_writev(ngx_connection_t *c, ngx_iovec_t *vec)
 
 eintr:
 
+#ifdef __wasi__
+    /* Coalesce small scatter/gather writes before entering a P2 stream.
+     * write() still reports the exact prefix accepted, so the caller's
+     * normal chain advancement also handles a short write or EAGAIN.
+     * Bound the temporary storage and keep large/single-buffer writes on
+     * the existing path. No data remains buffered after this call.
+     */
+    if (vec->count > 1 && vec->size <= 4096) {
+        u_char buffer[4096];
+        u_char *p = buffer;
+        for (ngx_uint_t i = 0; i < vec->count; i++) {
+            p = ngx_cpymem(p, vec->iovs[i].iov_base, vec->iovs[i].iov_len);
+        }
+        n = write(c->fd, buffer, vec->size);
+        goto eintr_done;
+    }
+    /* wasi-libc p2 的 writev 只写第一个 iov（实测），逐段 write 代替 */
+    {
+        ssize_t total = 0;
+        for (ngx_uint_t i = 0; i < vec->count; i++) {
+            ssize_t rc = write(c->fd, vec->iovs[i].iov_base, vec->iovs[i].iov_len);
+            if (rc == -1) {
+                if (total) { n = total; goto eintr_done; }
+                n = -1;
+                goto eintr_done;
+            }
+            total += rc;
+            if ((size_t) rc < vec->iovs[i].iov_len) break;
+        }
+        n = total;
+    }
+eintr_done:
+#else
     n = writev(c->fd, vec->iovs, vec->count);
+#endif
 
     ngx_log_debug2(NGX_LOG_DEBUG_EVENT, c->log, 0,
                    "writev: %z of %uz", n, vec->size);
